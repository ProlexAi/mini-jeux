# Journal — 2026-09-11

Session `claude-mini-jeux`, orchestrateur dédié en implantation, sur ordre de Matt relayé par
ProlexCore (D102, D104). Travail sur la branche `orchestration-2026-09-11` (garde-fou de
ProlexCore : aucun push, aucune fusion sur `main` ce soir).

## 21h — WP5 : kit agent-workflow 1.20.0 → 1.22.0, clé `agents` posée

Le prompt visait 1.21.0 (relevé de ~19h45) ; le socle avait déjà bumpé à 1.22.0 au moment de
cette session — mesuré par `installer.py --blanc` avant d'écrire, prémisse requalifiée plutôt
que suivie à l'aveugle.

- `installer.py . --agents claude-mini-jeux` : un seul geste couvre ce que le prompt
  découpait en deux (0.6 puis édition JSON manuelle de 0.7) — l'option `--agents` est neuve
  au socle 1.22.0. `--controle` : conforme, `agents = claude-mini-jeux, git`.
- **Écart mesuré avec le prompt** : `.workflow/local.json` est désormais dans
  `.workflow/.gitignore` (motif documenté dans le fichier : incident déjà payé avec
  `state.json`/`journal.jsonl` sur un `git checkout` de branche). Je ne l'ai **pas** commité,
  contrairement à ce que demandait le §0.7 du prompt écrit pour 1.20.0/1.21.0 — la garde A1
  lit le fichier local directement, pas besoin qu'il soit versionné pour mordre.
- Commit `3715908` : runtime seul (10 fichiers, pathspec explicite).
- Fédération confirmée : 4 dépôts au registre.
- Compte-rendu déposé : `2026-09-11-211834-claude-mini-jeux-wp5-kit-pose.md`.

## 21h — T-064 : `.env.example` posé, `.gitignore` corrigé en premier

- `git check-ignore -v .env` échouait avant écriture (mesuré) : aucune règle `.env`.
- **Piège d'instrument trouvé et évité** : un `.gitignore` en `.env` + `.env.*` +
  `!.env.example` (la variante « couvrir toute variante » que le prompt proposait en option)
  fait dire « ignoré » (exit 0) à `git check-ignore .env.example`, alors que
  `git status --ignored` confirme que le fichier reste suivi — reproduit sur un dépôt jetable
  avant de conclure. Gardé la règle simple (`.env` seul, nom exact) : elle satisfait la
  preuve de fin à la lettre (`.env.example` muet) et suffit, ce dépôt n'ayant aucune
  convention `.env.local`.
- 6 variables documentées sans valeur : `WORKFLOW_AGENT`, `WORKFLOW_CORRELATION`,
  `PROLEX_REGISTRE` (kit), `COMFYUI_INPUT_DIR` (assets IA), `DEPOT_DIR`/`BRANCHE`
  (`restauration-linux.sh`, jamais exécuté). `USER`/`USERNAME` écartés (repli interne de
  `_os_user()`, jamais une entrée de config). `Snake'on/index.html` confirmé sans aucune
  lecture d'environnement.
- Commit `a435296` : `.gitignore` + `.env.example` (pathspec explicite).
- Compte-rendu déposé : `2026-09-11-212124-claude-mini-jeux-d24-env-example-pose.md`.

## 21h — PARTIE B : rapport d'état, pose des surfaces, contradictions

Budget d'agents déclaré (0.5) : **zéro sous-agent**, tout en direct — guardé-fou D104
(« aucune session claude lancée »), et le travail de ce soir (lecture, confrontation, écriture
de documents) ne demande pas de contexte isolable d'un spécialiste.

- **Rapport d'état** (commit `387f2f8`) : 13 règles confrontées à la doctrine, 0 en « reprise
  au gabarit », critère d'arrêt sous le seuil — la passe continue. Défaut trouvé : `CLAUDE.md`
  n'importait ni `@SOUL.md` ni `@AGENTS.md` (contredit D32), `AGENTS.md` donc jamais chargé par
  aucune session ici. `JOURNAL/` et la contre-passe, pratiques réelles, absentes de toute
  surface d'ordres.
- **Coffre Obsidian** (commit `697b92c`) : versé depuis la matière `ProlexCore/SOCLE/profils-agents/coffres/mini-jeux/`
  (trouvée en lisant le dépôt, non citée dans le préambule du prompt), 9 notes + accueil.
  Emplacement choisi contre la recommandation du gabarit : **dans le dépôt, gitignoré et
  vérifié** — écart assumé et motivé par D104 (rien hors de `/home/matt/mini-jeux` ce soir).
  Preuve : `git status --ignored` le montre en `!!`, `git ls-files` en rend 0.
- **Trois surfaces posées** (commit `0091f17`, corrigé en `a90b3d0` et `72a14d3`) : `SOUL.md`
  (70 lignes), `CLAUDE.md` (95 lignes), `OUTILS.md` (187 lignes, entièrement re-mesuré, pas
  recopié). **R-09 joué deux fois, pas seulement écrit** : `controler-ton-claude-md.py`
  a d'abord refusé `SOUL.md` (31 lignes de prose hors chapeau — des puces repliées sur
  plusieurs lignes physiques, lues comme des paragraphes) puis `CLAUDE.md` (« Ne jamais
  retraiter… », tournure « ne … jamais » imperative, `BLOQUANT`) ; les deux corrigés et
  rejoués jusqu'au vert. Les trois rendent `0`, aucune ligne `VOIR`.
- **Témoin garde A1** : `WORKFLOW_AGENT=intrus … claim T-001 …` refusé (`NomNonAlloue`,
  `EXIT=1`), aucune réservation n'a fuité (`snapshot` toujours vide après).
- **Rapport de contradictions** (commit `2927a8e`, déposé à la boîte d'échange
  `2026-09-11-214001-…`) : 3 sections. Doctrine↔dépôt (condensation kit-hygiène, coffre
  en-dépôt plutôt que hors-dépôt, `AGENTS.md` pas enrichi) ; dépôt↔doctrine (témoin dans
  l'instrument, critère d'arrêt posé avant de commencer, marqueur de déploiement, contrôles
  sabotés — 4 candidats au canon) ; doctrine↔elle-même (`PROTOCOLE-AGENTS.md` se contredit sur
  la validation d'un spécialiste, `LANGUE-COMMUNE.md` cite T-098/T-105 comme ouvertes alors que
  D81 les a closes le même jour, « posé » (D66) et le déclencheur de retrait (D69) pas
  synchronisés sur le même jalon, plafond 200 lignes pas clair sur `OUTILS.md`).
- **Livrables HTML** (commit `adab443`) : les trois documents ci-dessus, rendus à côté de leur
  Markdown. Contraste **mesuré par calcul** (luminance relative WCAG : 6,23:1 le plus bas en
  clair, 7,71:1 en sombre) plutôt qu'au navigateur — un fichier local hors du dossier de projet
  ne s'ouvre pas en page interactive dans ce panneau ; équilibre des balises vérifié par script.

## RENDU À MATT

**[C]** Kit 1.22.0 fédéré, `.env.example` posé, trois surfaces (`SOUL.md`/`CLAUDE.md`/`OUTILS.md`)
posées et propres au contrôle de ton, coffre versé et vérifié hors suivi git, témoin de garde A1
joué et mordant. Tout sur la branche `orchestration-2026-09-11` — rien poussé, rien fusionné sur
`main` (garde-fou D104). `mini-jeux-projet.md` **non retiré** ce soir (D69 attend la fusion,
même précédent que KmopBudget).

**[Q]** Le coffre vit dans le dépôt (gitignoré) plutôt que hors dépôt comme le gabarit le
recommandait — contrainte de cette session (D104), pas un choix de fond. **Ma reco** : le
laisser ainsi tant que ça tient (le dépôt a déjà ce pattern pour deux autres familles de
fichiers), sauf si Matt préfère la garantie structurelle d'un emplacement hors dépôt — à
trancher une fois, pas à chaque session.

**[Q]** `PROTOCOLE-AGENTS.md` porte une phrase stale (« Matt valide ou améliore [la flotte] »)
qui contredit D66 trois paragraphes plus bas, dans le même fichier. **Ma reco** : la retirer ou
la faire pointer vers l'amendement — un correctif d'une ligne, sans enjeu de fond.

**[Q]** `LANGUE-COMMUNE.md` cite T-098/T-105 comme encore ouvertes alors que D81 les a closes le
jour même. **Ma reco** : mettre à jour la condition, même correctif mineur.

**[Q]** Le plafond « moins de 200 lignes » ne dit pas s'il s'applique à `OUTILS.md` — le seul
précédent posé (KmopBudget) fait 232 lignes, au-dessus, sans exception écrite. **Ma reco** :
l'exempter explicitement (nature « inventaire », plus longue par construction), pour ne pas
laisser un futur agent hésiter entre couper son `OUTILS.md` ou suivre le précédent.

**[F]** `mini-jeux-projet.md` à retirer une fois la branche de ce soir fusionnée sur `main`
(équivalent local de T-172) — prouver sa récupérabilité avant, comme la doctrine l'exige.

**[F]** Les pièges détaillés de `mini-jeux-projet.md` (service worker fantôme, Wayland, qualité
graphique adaptative) à fusionner dans `AGENTS.md` avant ce retrait, pour ne rien perdre — signalé
en `OUTILS.md` §7, pas fait ce soir (hors périmètre du prompt).

**[F]** Quatre candidats au `CANON/PIEGES-MESURES.md` de ProlexCore, listés dans le rapport de
contradictions (§ Dépôt contre doctrine) — à évaluer par ProlexCore, pas à moi de trancher pour
un canon qui n'est pas le mien.
