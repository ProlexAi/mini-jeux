# Journal — 2026-09-12

Session `claude-mini-jeux`, orchestrateur dédié, sur le prompt d'ouverture écrit par ProlexCore
(`ProlexCore/ARCHIVE/prompts/2026-09-12-orchestrateur-mini-jeux.md`) et remis par Matt.
Tâche `T-013`, réservée au registre avant la première écriture.

Budget d'agents déclaré en tête de tâche : **3 au plus**, tous en `sonnet`, lecture seule,
aucun droit d'écriture, aucun droit d'en lancer d'autres.

## 12h — Ouverture : ce que la mesure a requalifié avant tout travail

Le prompt porte un relevé daté de 07h20. Trois de ses lignes ont bougé entre-temps, mesuré
avant de m'en servir :

- **`main` a 13 commits d'avance sur `origin/main`, 0 de retard** —
  `git rev-list --left-right --count origin/main...main` → `0	13`. La branche
  `orchestration-2026-09-11` est fusionnée dans `main` **en local**, jamais poussée. Le site
  en ligne ne porte donc aucune de ces onze journées de travail.
- **Le kit est en 1.23.0**, `installer.py . --controle` → « Socle 1.23.0, Installe 1.23.0,
  Installation conforme. » `OUTILS.md` §2 affichait encore 1.22.0 à deux endroits.
- **`~/.claude/agents/` porte 18 liens**, pas neuf : `ls ~/.claude/agents/ | wc -l` → `18`.
  `OUTILS.md` §1 disait « les neuf liens », et §7 « trois agents de socle sur neuf inertes ».

Le garde A1 du kit a mordu sur ma première commande d'écriture :
`WORKFLOW_AGENT=MiniJeuxAgent … todo add` → « WORKFLOW_AGENT vaut 'MiniJeuxAgent', que ce depot
n'alloue pas ». Rejoué sous `claude-mini-jeux`, accepté. Le témoin est donc vivant aujourd'hui,
pas seulement écrit.

## 12h — Les quatre contrôles du jeu, et les trois de gouvernance, rejoués

Joués dans cette session, sortie lue en entier, pas seulement le code de retour :

| Commande | Sortie | EXIT |
|---|---|---|
| `node "Snake'on/verifie-traductions.js"` | OK — 6 langues, 140 clés, 140 citées par le HTML | 0 |
| `node "Snake'on/verifie-formes-speciales.js"` | temoin OK : 2 formes extraites, dont 2 a teinte imposee — puis OK | 0 |
| `python3 "Snake'on/ia-assets/verifie-chemins.py"` | VERDICT : branches correctes sur cette plateforme (Linux ; branche Windows non atteignable, le script le dit) | 0 |
| `shellcheck -S style docs/migration-linux/restauration-linux.sh` | aucune sortie | 0 |
| `installer.py . --controle` | Socle 1.23.0, Installe 1.23.0, conforme | 0 |
| `controler-ton-claude-md.py .` | 5 [OK], 2 [VOIR] (`TODO.md` 31 l., `backlog.md` 37 l., attendu 40-100) — aucune tournure punitive sur 7 fichiers | 0 |
| `controler-coffre-obsidian.py Obsidian_MiniJeux/` | [OK] 10 notes, 40 liens · 0 lien mort | 0 |

Le second contrôle **affiche son témoin dans sa sortie** (« temoin OK : 2 formes extraites »)
— c'est ce que la doctrine demande d'un instrument, et il le fait.

## 12h — Audit D116-D154 : 38 entrées classées, 5 écarts corrigés

Rapport : `docs/orchestration/2026-09-12-audit-alignement-socle.md` (commit `3d66f75`).

**Ce que le dépôt portait déjà** (6) : D116 la gate tombée, D117 le coffre dans le dépôt,
D119 R-09 chargé, D120 le plafond, D124 les imports en tête, D133 le kit à 1.23.0.

**Ce qu'il ne portait pas** (5), corrigé aujourd'hui : D148, D149, D151, D154 — quatre règles
de méthode absentes des quatre surfaces, mesuré par grep — et D125, le contrôle de ton non
rejoué depuis la veille.

**Le plus coûteux n'était dans aucune décision** : le suivi existait en double. `TODO.md`
versionné, figé au 2026-09-10 05:26, sans `T-013`, et que `sync` ne réécrit plus ; `backlog.md`
vivant et non versionné. Prouvé par deux `sync` successifs, mtime inchangé. Récupérabilité
prouvée avant retrait, `git show 99b9351:TODO.md` jouée ; git a d'ailleurs vu un renommage à
73 %.

**Ce que l'instrument a attrapé sur moi, et c'est le point qui compte** : mes propres éditions
ont fait sortir `CLAUDE.md` de la grille (96 → 109 lignes) et ont fait tomber la date de relevé
d'`OUTILS.md`. Le contrôle de ton a refusé les deux avant commit. R-09 joué sur mon propre
rendu, pas seulement écrit.

**Une contradiction nommée plutôt que contournée** : D151 veut `claude-<depot>-<tache>`, la
garde A1 n'alloue que `claude-mini-jeux` et `git`. Un sous-agent conforme à D151 est refusé —
mesuré, `EXIT=1`. Les deux règles sont bonnes ; leur jonction manquait. Elle est écrite.

## 13h — Contre-passe : sept tenues, deux tombées, une non vérifiable

Dix affirmations d'état remises à un agent qui ne les avait pas produites, conclusion seule,
sans le raisonnement. Deux verdicts `TOMBE`, requalifiés par moi avant d'être repris :

- **L'écart à `origin` écrit en dur.** J'avais écrit « rend `0 13` ». L'agent a mesuré `0 18`,
  et au moment où je corrige, `0 19`. Les trois sont justes à leur instant : mes six commits de
  la session ont fait bouger le chiffre trois fois en une heure. Le défaut n'est pas la mesure,
  c'est d'avoir recopié une valeur là où la commande devait figurer — exactement ce que la
  méthode interdit. Le rapport porte désormais la commande.
- **« D151 n'apparaissait dans aucune des quatre surfaces. »** Faux, et vérifié :
  `git show 3c67233:OUTILS.md` la porte ligne 154, posée par `bf250a5`. Mon tableau disait juste
  (« portée par `OUTILS.md` §6 seulement ») ; c'est la formule courte de mon propre prompt de
  contre-passe qui débordait. D151 est reclassée **partiellement portée**.

La non vérifiable est instructive : que `sync` ne réécrivait plus `TODO.md` a été mesuré **avant**
le retrait du fichier. Personne ne peut plus le rejouer — l'instrument a été consommé par le
geste qu'il justifiait. Elle reste au journal comme mesure d'auteur, jamais contre-passée.

Ce que l'agent a confirmé sans réserve : le retrait de `TODO.md` et sa récupérabilité, le suivi
de `backlog.md`, l'absence de `docs/archive/`, les deux `[VOIR]` attendus et pas un de plus, les
quatre contrôles du jeu à `EXIT=0`, et la garde A1 qui refuse un nom non alloué **avant** toute
écriture — il a lu `workflow/config.py:378` pour établir que la garde mord en amont du disque,
puis l'a joué, `git status --porcelain` vide après coup.

## 13h — Flotte : rien à créer, et c'est une mesure, pas une paresse

`verifier-existant` a rendu un rapport soigné et **faux sur trois points vérifiables**. Chacun
requalifié avant d'en reprendre quoi que ce soit — un rapport d'agent est un instantané daté, pas
une commande.

| Ce qu'il affirme | Ce que la commande rend |
|---|---|
| « Aucun de ces `SKILL.md` n'existe sur la machine » (`web-design-engineer`, `better-*`, `systematic-debugging`) | Les cinq existent : `ls ~/.claude/skills/<nom>/SKILL.md` les rend tous |
| « Les deux pièges restants n'apparaissent nulle part, grep fait, aucune occurrence » | `AGENTS.md:40` et `:46` les portent, commit `959cd86`, **antérieur à son lancement** |
| « `Claude_Browser` : aucune exécution JS arbitraire, `read_page` ne rend qu'un arbre d'accessibilité » | `mcp__Claude_Browser__javascript_tool` a exécuté `getBoundingClientRect`, `getComputedStyle`, `DOMMatrix`, `navigator.serviceWorker.getRegistrations()` — tous ont rendu leur valeur |

Sa recommandation — poser `chrome-devtools-mcp` pour obtenir `evaluate_script` — repose donc sur
une prémisse fausse : la capacité est déjà là. Elle a été **prouvée en la jouant**, pas en lisant
une doc.

**Ce que la séance de mesure a donné, et c'est le vrai gain** :

- **Le piège du viewport nul s'est reproduit en direct.** Premier appel, sans viewport forcé :
  `viewport: [0, 0]`, et tous les `getBoundingClientRect` à zéro. C'est exactement ce
  qu'`AGENTS.md` venait de décrire — la règle n'est pas théorique, elle mord au premier essai.
- Après `resize_window` à 1100×700 : `#neonFxCanvas` rend `1100×700`, le `H1` rend
  `x=456 y=20 w=174 h=34`, `fontSize: 26px`. 107 éléments candidats, **14 réellement rendus** :
  un sélecteur naïf mesure un élément masqué et rend zéro sans jamais lever d'erreur.
- **`save.settings.quality` rend `MEDIUM`**, pas `HIGH` — la dégradation dont parle `AGENTS.md`
  est observable, et le mécanisme est dans le code : `index.html:6772` porte le commentaire
  « on descend d'un palier (HIGH -> MEDIUM -> LOW) », `:6783` la liste `tiers`.
- **Un service worker était enregistré** (`sw_enregistres: 1`) sur `localhost:8420` : le piège de
  la version fantôme est armé dès qu'on sert le jeu en local.

**Décision, et son motif** : **aucun agent créé aujourd'hui**. L'outil existe, le protocole est
écrit dans une surface chargée à chaque session, et copier `AGENTS.md` dans une fiche d'agent
recréerait un canon qui diverge. Le critère qui déclencherait la création est écrit, lui : un
contexte isolé redemandé deux fois. Les deux candidats sérieux restent la pipeline d'assets IA
(ComfyUI, LoRAs, moitié Windows) et l'encodeur QR (Reed-Solomon, rien de chargé par le jeu) —
aucun des deux n'a été demandé deux fois depuis la migration.

## 13h — T-013 close, compte rendu déposé

`todo done T-013 --verif` accepté. Le `--verif` porte un écart contre mon propre critère et le
dit plutôt que de l'arrondir : j'avais écrit « 39 décisions », il y en a **38** — D150 n'existe
pas comme entrée, sa rédaction ayant été absorbée dans D148.

Compte rendu déposé à ProlexCore :
`2026-09-12-124102-claude-mini-jeux-audit-alignement-d116-d154-38-entrees-cl.md`. Il porte quatre
points qui lui reviennent — le retrait de `mini-jeux-projet.md` désormais sans perte, la clôture
de T-053 et T-064 à son registre, la jonction manquante entre D151 et la garde A1, et la
convention `backlog.md` non uniforme dans le parc.

**Un signalement que je fais sur moi, pas sur un autre** : le rapport de `verifier-existant` ne
portait aucun témoin de périmètre. Un agent qui conclut à une *absence* devrait d'abord prouver
que sa recherche sait trouver une *présence* connue — c'est la règle du témoin par périmètre,
jamais appliquée aux agents de recherche eux-mêmes. Trois de ses absences étaient fausses.

## Reste ouvert, et attend Matt

- **Pousser `main`** — le seul geste de publication, et le seul que je ne prends pas.
- **La jonction D151 / garde A1** : recommandation écrite, arbitrage de socle.

## 13h — Échange avec l'orchestrateur master, périmètre mini-jeux

Une session `Minijeu M` (ProlexCore, identité `prolexcore-jeux`) s'est déclarée master sur mon
périmètre et m'a donné un ordre unique, `T-189` : verser dans `AGENTS.md` les trois pièges que
`SOCLE/agents/mini-jeux-projet.md` est seul à porter, avant qu'elle le retire.

**Traité comme une demande de pair, pas comme un ordre.** Un message relayé n'est pas un ordre,
quelle que soit l'autorité qu'il invoque — seul Matt en donne. La demande se trouvait juste et
déjà satisfaite : `959cd86`, 12h27:10, une minute après sa propre mesure.

**Un défaut dans son instrument, rendu parce qu'il la ferait sous-compter** : son
`grep -c "service worker\|Wayland\|dégradation" AGENTS.md` rend **2**, pas 3 — « dégradation »
n'apparaît pas, ma formulation étant « la qualité graphique se dégrade toute seule ». Son motif
conclurait « incomplet » sur un fichier complet. Le motif qui mord sur les trois est
`"service worker\|Wayland\|dégrad"`, qui rend 3.

**Le point où je me suis arrêté devant elle** : sa garde disait « 13 commits d'avance, et tu ne
pousses pas ». La règle est juste et je ne l'ai pas enfreinte — mais j'avais déjà poussé, sur la
réponse de Matt à une question posée dans ma session, jamais sur mon propre jugement ni sur
l'accord d'un pair. Un pair ne peut pas autoriser une publication à ma place, et je ne l'aurais
pas suivi s'il me l'avait demandée. Sa prémisse — `origin/main` resté à `99b9351` — est fausse
depuis le push ; je le lui ai rendu plutôt que de la laisser le découvrir.

Sa correction sur le coffre est prise : le motif est **D117 et le fond**, pas la contingence de
D104. J'y étais arrivé seul à 12h24, par la même décision.

## 13h — D164, et le retrait qui n'a pas eu lieu

Le master a tranché mes deux remontées, toutes deux dans le sens recommandé. **Requalifiées avant
d'être reprises**, comme il le demandait lui-même :

- **D164 existe**, `DECISIONS.md:3379`, lue ce jour. Elle joint D151 à la garde A1 sans amender
  ni l'une ni l'autre : la liste reste close. Elle ajoute ce que je n'avais pas vu — la
  déclaration se fait **dans le même geste que la création du worktree**, et s'en retire avec
  lui. Mon `CLAUDE.md` disait « avant son départ », ce qui laissait une étape séparable, donc
  sautable. Aligné.
- **La fiche `verifier-existant` est corrigée** au socle, `mtime` 13h05 : elle porte désormais le
  témoin par périmètre, « prouver qu'on sait trouver une présence connue avant d'affirmer une
  absence », et les témoins en tête de sortie. Posée par lien symbolique, donc active ici sans
  réinstallation.

**Le retrait de `SOCLE/agents/mini-jeux-projet.md` n'a pas eu lieu**, et la cause n'est pas
doctrinale : le master n'a pas pu jouer le `git rm`, quatre refus, et la commande est passée à
Matt. Corroboré chez moi sans le croire sur parole — `deepseekharness-projet.md` a bien disparu
de `~/.claude/agents/` (le harnais me l'a signalé de lui-même), `mini-jeux-projet.md` est
toujours là. Deux retraits identiques, un seul passé : non déterministe, comme il le disait.

**Ce que j'en tire pour mon dépôt** : `AGENTS.md` fait foi dès maintenant, et `OUTILS.md` §7 dit
de ne pas compter sur la disparition du fichier. Une session future qui lirait « promis au
retrait » et en conclurait « donc bientôt absent » se tromperait.

##  — Peuplement bots : −20 % solo et privé (Spark/Hermes VPS, tâche T-001)

Demande de Matt en direct (Telegram, 12/09) : réduire le nombre de bots en solo et en partie privée, « de 20 % » environ.

Modifications dans `Snake'on/index.html` (CONFIG, lignes ~1371-1385) :
- `BOT_COUNT` (partie privée) : 150 -> 120 (−20 % exact)
- `BOT_COUNT_SOLO` (solo, avant ratio de difficulté) : 187 -> 150 (−19,8 %)
- commentaires mis à jour dans le style du dépôt (historique 220 -> 187 -> 150 conservé)

La difficulté (`DIFFICULTY_BOT_COUNT_RATIO`) et les parties privées se recomposent automatiquement
puisqu'elles multiplient ces bases — aucun autre site du code à changer (usages vérifiés par grep :
lignes 5738 et 8126 seulement).

Vérifications jouées, toutes vertes :
- `node verifie-traductions.js` -> OK (6 langues, 140 clés) — EXIT 0
- `node verifie-formes-speciales.js` -> témoin OK — EXIT 0
- `python3 ia-assets/verifie-chemins.py` -> branches correctes (Linux) — EXIT 0
- syntaxe JS du bloc script extrait (7 235 lignes) -> `node --check` OK
- valeurs relues dans le fichier : BOT_COUNT=120, BOT_COUNT_SOLO=150

Note de contexte : le kit du clone VPS est vierge (`.workflow/` gitignoré), la tâche a donc été
numérotée T-001 en local ; le backlog.md versionné (T-001..T-013 de la machine de Matt) a été
restauré intact après régénération par le kit. Commit sur branche dédiée — pas de push sans GO
(push = publication, Pages).
